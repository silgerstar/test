# 학습 도우미 인증 플로우 기반 개발 계획서 (Cursor용)

> 목적: GPTs Actions와 연동되는 할 일 관리 API 서버를 구축하고, 사용자가 챗봇 대화에 업로드한 인증 이미지를 챗봇이 판정하여 서버의 할 일을 완료(승인)로 전환하는 흐름을 구현한다. (알림 발송 로직은 제외)

---

## 프로젝트 목표 (업데이트 반영)

* GPTs Actions가 호출하는 **할 일 관리 API** 제공
* 사용자는 챗봇 대화에 **“한 일과 그에 대한 인증 이미지”** 를 업로드
* 챗봇은 서버에서 **미완료 목록 조회 → 해당 할 일 매칭 → 인증 방법 기준으로 판정(gpt5의 멀티모달 기능을 이용해 챗봇이 자체적으로 판단)**
* 챗봇이 **완료라고 판단**하면 서버의 해당 할 일을 **APPROVED**(완료)로 전환하고, 판정 근거를 로그로 저장
* 마감 도래 시 워커가 **EXPIRED** 로만 전환 (알림 발송 제외)

---

## 기술 스택

* **Python 3.11**, **Flask**, **SQLAlchemy** (+ Alembic)
* **PostgreSQL** (Render PostgreSQL Add-on)
* **파일 업로드**: GPT 내부 처리(서버는 `proof_url`만 저장)
* **배포**: Render (Web Service + Background Worker)
* **시간대**: 서버는 UTC 저장, 사용자 표시는 `users.tz` (기본 `Asia/Seoul`)

---

## 인증 플로우(요구사항 반영)

1. **사용자 제출**: 사용자는 미리 등록해 둔 할 일(문자열)을 기준으로, 완료 선언(예: “수특 물리1 p.10~20 끝!”)과 **인증 이미지(들)** 를 챗봇에 업로드한다.
2. **미완료 목록 조회**: 챗봇이 API에 `GET /v1/tasks?user_id=...&state=PENDING&q=...` 를 호출해 후보 목록을 가져온다.
3. **매칭**: 제목/마감/인증방법을 기준으로 **단일 Task** 를 선택한다. 애매하면 사용자에게 후보를 보여주어 선택받는다.
4. **증빙 업로드(GPT 내부)**: 챗봇이 사용자가 올린 파일을 GPT 내부에 저장하고 **공유 가능한 파일 URL(또는 첨부 asset id)** 을 확보한다.
5. **GPT 자체 판정(멀티모달)**: 챗봇(GPT-5)이 **인증방법(verify_method)** 을 해석하고 이미지/OCR 정보를 결합하여 **합/불합 판정**을 수행한다. 판정 결과로 `verdict`(true/false), `score`(신뢰도), `reasons`(근거 요약), `raw_features`(검출된 페이지/키워드 등)을 생성한다. 여러 장 업로드 시 모두 평가한다.
6. **상태 반영**: 챗봇이 `POST /v1/tasks/{id}/verify-attempt` 를 호출해 판정 결과와 `proof_url`을 기록한다. 서버는 시도 로그를 저장하고, `verdict=true` 인 경우 **동일 트랜잭션에서 **`` 로 전환한다. `verdict=false` 면 상태는 유지되며 챗봇은 사용자에게 **추가 인증**(예: 페이지 범위 보이도록 재촬영)을 요청한다.
7. **만료 처리(워커)**: 마감 시간이 지난 `PENDING` 작업은 워커가 주기적으로 스캔해 `` 로 전환한다(알림 발송 제외).

> 서버는 판정을 수행하지 않고 **GPT의 판정 결과를 신뢰**하여 상태를 전환한다. 모든 판정은 **verification_attempts** 로 감사 로그에 남긴다.

---

## 데이터 모델 (상태 머신 단순화)

상태: `PENDING` → `APPROVED` → `EXPIRED`

* **마감 스캔**: `due_at <= now()` 이고 `state='PENDING'` 인 항목은 `EXPIRED` 로 전환
* **챗봇 판정 실패**: 상태 변화 없음, 다만 `verification_attempts` 로그만 저장

### 스키마 (PostgreSQL)

```sql
create extension if not exists pgcrypto;

create table users (
  id uuid primary key default gen_random_uuid(),
  external_user_id text unique not null,
  tz text default 'Asia/Seoul',
  created_at timestamptz default now()
);

create table tasks (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references users(id) on delete cascade,
  title text not null,             -- 예: "수능특강 물리1 p.10~20"
  verify_method text not null,     -- 예: "해당 페이지 풀이 촬영"
  due_at timestamptz not null,     -- UTC
  state text not null check (state in ('PENDING','APPROVED','EXPIRED')),
  created_at timestamptz default now(),
  updated_at timestamptz default now()
);
create index on tasks (user_id, state);
create index on tasks (user_id, due_at);

-- 챗봇 판정 시도의 감사 로그
create table verification_attempts (
  id uuid primary key default gen_random_uuid(),
  task_id uuid references tasks(id) on delete cascade,
  attempted_at timestamptz default now(),
  proof_url text not null,               -- 업로드된 이미지/파일 URL
  verdict boolean not null,              -- 챗봇 판정: 합/불합
  score numeric(5,2),                    -- 판정 신뢰도(옵션)
  reasons text,                          -- 사유 요약 (예: 페이지 일치, 필기 확인 등)
  raw_features jsonb                     -- 추출된 페이지 번호/키워드/EXIF 등
);
```

---

## API 설계 (알림 제외)

**Base URL**: `/v1`

### 사용자/할 일 기본

* `POST /users`

  * 요청: `{external_user_id, tz?}`
  * 응답: `{user_id}`

* `POST /tasks`

  * 요청: `{user_id, title, verify_method, due_at_iso}`
  * 응답: `{task_id, state: 'PENDING'}`

* `GET /tasks`

  * 쿼리: `user_id`(필수), `state?`, `q?`, `due_before?`, `due_after?`
  * 응답: `{items:[{id,title,verify_method,due_at_iso,state}]}`
  * 메모: `q`는 제목 부분일치/간단 유사도(ILIKE + 공백 정규화)

### 파일 업로드(챗GPT 내부 처리)

* 업로드는 **API 서버가 관여하지 않으며**, 사용자가 챗봇 대화에 이미지를 올리면 **GPT 내에서 처리**됩니다.
* API 서버는 업로드 엔드포인트를 제공하지 않습니다. 따라서 ``**는 사용하지 않습니다**.
* GPT는 자체 스토리지/호스팅을 통해 **공유 가능한 파일 URL(또는 첨부 asset id)** 을 확보하고, `POST /v1/tasks/{id}/verify-attempt` 호출 시 `` 로만 전달합니다.
* 서버는 전달받은 `proof_url`을 `verification_attempts.proof_url` 에 저장하고, 판정 결과(`verdict`)에 따라 상태 전환만 수행합니다.

### 인증 판정 시도 기록 (상태 전환 포함)

* `POST /tasks/{id}/verify-attempt`

  * 요청 예:

    ```json
    {
      "proof_url": "https://r2/.../image.jpg",
      "verdict": true,
      "score": 0.92,
      "reasons": "p.10~20 범위와 일치, 이름 표기 확인",
      "raw_features": {"pages":[10,11,12], "found_keywords":["물리1","p.10"]}
    }
    ```
  * 동작: 항상 시도 로그 저장. `verdict=true`면 동일 트랜잭션에서 `tasks.state='APPROVED'` 로 전환.

### 만료 처리(크론/워커)

* 내부용(옵션) `POST /internal/cron/expire`

  * 동작: `due_at <= now()` & `state='PENDING'` → `EXPIRED` 전환
  * 응답: `{processed: N}`
  * 보호: `Authorization: Bearer <CRON_TOKEN>`

### 보안

* 공용 라우트: `X-API-Key` (GPTs Actions용)
* 내부 크론: Bearer 토큰

---

## 디렉토리 구조(초안)

```
study-assistant/
  app/
    __init__.py
    config.py
    db.py
    models.py
    schemas.py
    routes/
      users.py
      tasks.py
      internal.py
    services/
      expirations.py    # expire_overdue_tasks()
    utils/
      time.py
      auth.py
  migrations/
  tests/
    test_tasks_flow.py
  openapi.yaml
  Dockerfile
  render.yaml
  requirements.txt
  README.md

      internal.py
    services/
      storage.py        # presign
      expirations.py    # expire_overdue_tasks()
    utils/
      time.py
      auth.py
  migrations/
  tests/
    test_tasks_flow.py
  openapi.yaml
  Dockerfile
  render.yaml
  requirements.txt
  README.md
```

---

## 상태 전이 규칙(간단)

* 생성: `PENDING`
* `verify-attempt(verdict=true)`: `APPROVED`
* 마감 크론: `PENDING` + `due_at<=now` → `EXPIRED`
* `APPROVED` / `EXPIRED` 는 불변 (관리자 되돌리기 엔드포인트는 초기 생략)

---

## OpenAPI 요약 (GPTs Actions 연결)

```yaml
openapi: 3.0.3
info: { title: Study Assistant API, version: 0.2.0 }
servers: [{ url: https://<render-url> }]
components:
  securitySchemes:
    apiKeyAuth: { type: apiKey, in: header, name: X-API-Key }
    bearerAuth: { type: http, scheme: bearer }
paths:
  /v1/tasks:
    get:
      security: [{ apiKeyAuth: [] }]
      parameters:
        - { name: user_id, in: query, required: true, schema: { type: string, format: uuid } }
        - { name: state, in: query, schema: { type: string, enum: [PENDING, APPROVED, EXPIRED] } }
        - { name: q, in: query, schema: { type: string }, description: "title 부분일치 검색" }
        - { name: due_before, in: query, schema: { type: string, format: date-time } }
        - { name: due_after, in: query, schema: { type: string, format: date-time } }
      responses: { "200": { description: OK } }
    post:
      security: [{ apiKeyAuth: [] }]
      responses: { "200": { description: OK } }
  /v1/tasks/{id}/verify-attempt:
    post:
      security: [{ apiKeyAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: "logged; state may become APPROVED" } }
  /v1/internal/cron/expire:
    post:
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: OK } }
```

---

## Render 배포 메모

* **Web Service**: `gunicorn "app:create_app()" --bind 0.0.0.0:10000`
* **Worker(크론)**: `python -m app.workers.expire_jobs` (1~5분 주기)
* **DB**: Render PostgreSQL
* `render.yaml` 예시:

```yaml
services:
  - type: web
    name: study-assistant-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "app:create_app()" --bind 0.0.0.0:10000
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: study-assistant-db
          property: connectionString
  - type: worker
    name: study-assistant-expirer
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.workers.expire_jobs
databases:
  - name: study-assistant-db
```

---

## Cursor 작업 지시(프롬프트)

* Flask+SQLAlchemy 스캐폴딩 생성
* 테이블: `users`, `tasks`, `verification_attempts` + 인덱스 정의, Alembic 마이그레이션 생성
* 라우트 구현:

  * `GET/POST /v1/tasks`
  * `POST /v1/tasks/{id}/verify-attempt`

    * `POST /v1/internal/cron/expire`
* 서비스: `expirations.expire_overdue_tasks()`
* 미들웨어: `X-API-Key` 검사, 내부 Bearer 검사, 요청ID 로깅
* 테스트:

  1. 생성→미완료 목록 조회→verify-attempt(verdict=false) 후 상태 유지
  2. verify-attempt(verdict=true) 후 `APPROVED` 전환
  3. 마감 경과 후 `EXPIRED` 전환

---

## 판정 품질 팁(챗봇 측 구현 힌트)

* **제목 정규화**: 공백/기호 제거, 교재명 표준화, 숫자 범위(p.10~20) 파싱
* **비전/OCR 힌트**: 페이지 번호, 교재 키워드(“물리1”), 문항 번호 탐지, 필기 흔적 여부
* **신뢰도 스레숄드**: `score >= 0.85` 시 합격, 미만이면 추가 인증 요청
* **근거 로깅**: `reasons`, `raw_features`를 저장하여 사후 감사/튜닝에 활용

---

## 환경변수(.env 예시)

```
FLASK_ENV=production
DATABASE_URL=postgresql+psycopg://user:pass@host:5432/db
APP_SECRET=changeme
API_KEY=changeme-actions        # GPTs Actions에서 사용
CRON_TOKEN=changeme-cron        # /internal/cron/expire 보호
```

---

## 초기 개발 순서

1. 프로젝트 스캐폴딩 + Dockerfile/requirements
2. 모델/마이그레이션 → 로컬 DB로 단위 테스트
3. `/v1/tasks` (create/get/list) → `verify-attempt`
4. `/uploads/sign` 스텁(로컬은 mock로 시작)
5. 만료 워커 + 내부 엔드포인트
6. OpenAPI 정리 → GPTs Actions 연결
7. Render 배포 및 실제 흐름 점검
